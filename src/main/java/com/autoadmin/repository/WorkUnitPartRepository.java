package com.autoadmin.repository;

import com.autoadmin.entity.WorkUnitPart;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WorkUnitPartRepository extends JpaRepository<WorkUnitPart, Long> {
}
